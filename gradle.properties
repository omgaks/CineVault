# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# FIX: added for Room's KSP annotation processor. This project uses AGP's
# newer "built-in Kotlin" compilation (no separate org.jetbrains.kotlin.android
# plugin — just org.jetbrains.kotlin.plugin.compose for Compose itself).
# KSP's Gradle plugin registers its generated-sources folder through the
# older kotlin.sourceSets DSL, which AGP's built-in Kotlin mode rejects by
# default with: "Using kotlin.sourceSets DSL to add Kotlin sources is not
# allowed with built-in Kotlin... To suppress this error, set
# android.disallowKotlinSourceSets=false in gradle.properties" — that
# exact suggested fix, copied directly from AGP's own error message, not
# a workaround found elsewhere.
android.disallowKotlinSourceSets=false
